// BottomNav is not used in RationSmart — navigation is via the left drawer (NavDrawer).
// This file is kept as a stub to prevent import errors from legacy references.
export default function BottomNav() {
  return null;
}
